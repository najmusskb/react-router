import React from 'react';
import Header from '../Header/Header';

const Barighor = () => {
    return (
        <div>
            <Header></Header>

            <h1>this is barighot</h1>

        </div>
    );
};

export default Barighor;